eureka:
  client:
    service-url:
      defaultZone: http://jnick.xyz:8762/eureka
    register-with-eureka: false
    fetch-registry: false
  server:
    enable-self-preservation: false
spring:
  application:
    name: eureka-server
server:
  port: 8762